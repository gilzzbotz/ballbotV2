// import chalk from 'chalk';
// const p = console.log;

// export default async (parsing) => {
// 	if (parsing.key.fromMe) return
// 	p(chalk.bgMagenta(parsing.cmd.startsWith(parsing.preff) ? 'PERINTAH' : 'PESAN')+chalk.bgCyan(parsing.isGc ? `GROUP ${parsing.chat}` : `PRIBADI ${parsing.chat}`) + chalk.magenta(parsing.cmd.substr(0, 100)));
// }